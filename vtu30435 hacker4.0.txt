import java.io.*;
import java.util.*;

interface PerformOperation {
    boolean check(int a);
}

class MyMath {

    public static boolean checker(PerformOperation p, int num) {
        return p.check(num);
    }

    PerformOperation isOdd() {
        return new PerformOperation() {
            public boolean check(int n) {
                return n % 2 != 0;
            }
        };
    }

    PerformOperation isPrime() {
        return new PerformOperation() {
            public boolean check(int n) {
                if (n < 2)
                    return false;

                for (int i = 2; i * i <= n; i++) {
                    if (n % i == 0)
                        return false;
                }
                return true;
            }
        };
    }

    PerformOperation isPalindrome() {
        return new PerformOperation() {
            public boolean check(int n) {
                int original = n;
                int reverse = 0;

                while (n > 0) {
                    reverse = reverse * 10 + n % 10;
                    n = n / 10;
                }

                return original == reverse;
            }
        };
    }
}

public class Solution {

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        MyMath ob = new MyMath();

        int T = sc.nextInt();

        while (T-- > 0) {
            int condition = sc.nextInt();
            int number = sc.nextInt();

            if (condition == 1) {
                System.out.println(MyMath.checker(ob.isOdd(), number) ? "ODD" : "EVEN");
            } 
            else if (condition == 2) {
                System.out.println(MyMath.checker(ob.isPrime(), number) ? "PRIME" : "COMPOSITE");
            } 
            else if (condition == 3) {
                System.out.println(MyMath.checker(ob.isPalindrome(), number) ? "PALINDROME" : "NOT PALINDROME");
            }
        }

        sc.close();
    }
}

Input (stdin)
5
1 4
2 5
3 898
1 3
2 12

Your Output (stdout)
EVEN
PRIME
PALINDROME
ODD
COMPOSITE

Expected Output
EVEN
PRIME
PALINDROME
ODD
COMPOSITE